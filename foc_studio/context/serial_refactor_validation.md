# 串口精简验证记录

日期：2026-09-14。修改前基线：`dd05a39fa7dc44af4f342833702ac97ed61153e9`。

## 自动验证

- `flutter analyze --no-pub`：通过。
- 删除测试代码前，`flutter test --no-pub` 的 12 项软件回归测试通过；真机测试未执行。测试代码现已按要求删除，本记录保留历史验证结果。
- `flutter build windows --debug --no-pub`：通过，已生成 Windows Debug 应用。
- `git diff --check`：通过。
- 覆盖全部分包位置、粘包、噪声、坏长度、CRC 恢复、短写、连续零写、写异常、调参整组拒绝与顺序、有界历史、重连清空半帧/队列、丢弃未投递的旧字节和消息、后台启动失败/退出、重复关闭和扫描超时。

## 快照微基准

基线源文件从上述提交读取，当前实现与基线同时运行在 Flutter 测试进程中。
两者输入相同的 72,400 字节，填满四类各 1200 条遥测和 500 条日志；预热 100 次，每轮构建 1000 次快照，共五轮。

首次不启用 VM service 的构建耗时（微秒）：

| 实现 | 五轮结果 | 中位数 |
| --- | --- | --- |
| 基线 | 45659 / 44985 / 41035 / 40478 / 41879 | 41879 |
| 当前 | 641 / 586 / 435 / 474 / 416 | 474 |

另一次启用 VM service 的测试中，用 `getAllocationProfile(gc: true)` 比较保留 1000 份快照前后的存活对象大小：

- 基线 `_ImmutableList` 增量：42,561,456 字节；当前同类增量未超过 10,000 字节的输出阈值。
- 基线 `FocSnapshot` 增量：224,000 字节；当前：176,000 字节。
- 该测量包含 VM service 和测试进程的背景分配，不能当作整个 App 的内存节省量。结构上普通快照已经没有历史列表字段。
- 计时只覆盖快照构建，不包括跨 isolate 传输、UI 绘制或串口 I/O；测试模式结果不等同于 Release 性能。

本次临时基准测试脚本已删除，以上数据为删除前的验证结果。

## 真机验证

系统枚举到 COM4 USB 设备和四个 com0com 虚拟端口，用户确认 COM4 为目标 MCU，波特率待提供。
物理拔插、持续吞吐及 Windows 原生资源长期占用尚未验证；现有跳过 Windows `SerialPort.dispose()` 的兼容措施保留。
